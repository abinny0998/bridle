using UnityEngine;

public class SFXSound : MonoBehaviour
{
    private static SFXSound instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
